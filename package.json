{
  "name": "ycard",
  "version": "1.0.0",
  "description": "A internationalized YAML version of iCard and x500 LDAP for humans",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/lsp.js",
    "test": "jest",
    "lint": "eslint src/**/*.ts",
    "generate-openapi": "npm run build && node scripts/generate-openapi.js"
  },
  "keywords": ["yaml", "vcard", "ldap", "contacts", "organization"],
  "author": "mieweb",
  "license": "MIT",
  "devDependencies": {
    "@types/js-yaml": "^4.0.5",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "js-yaml": "^4.1.0",
    "vscode-languageserver": "^9.0.0",
    "vscode-languageserver-textdocument": "^1.0.0"
  }
}
